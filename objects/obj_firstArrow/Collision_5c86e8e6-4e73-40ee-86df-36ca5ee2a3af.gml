visible = 0;


/*initialPortX = view_xport[0];
initialPortY = view_yport[0];
initialPortW = view_wport[0];
initialPortH = view_hport[0];*/

alarm[0] = 1;

/*for(i = 0; i <= default_zoom_height / 2; i++)
	{
		zoom_width--;
		zoom_height--;
		zoom_x++;
		zoom_y++;
		camera_set_view_size(view_camera[0], zoom_width, zoom_height);
		camera_set_view_pos(view_camera[0], zoom_x, zoom_y);
	}
	*/

